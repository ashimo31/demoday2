<!DOCTYPE html>
<html>
<head>
  <title>Agribook</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>

<header>
  <ul style="list-style:none;">
    <li><%= link_to 'トップページ', root_path %></li>
   <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">メニュー <span class="caret"></span></a>
    <ul class="dropdown-menu">
      <li><%= link_to 'お知らせ一覧', tasks_path if user_signed_in?%></li>
      <li><%= link_to '参加者一覧', users_path if user_signed_in? %></li>
      <li><%= link_to 'お願い一覧', works_path if user_signed_in? %></li>
      <li><%= link_to 'お願いしている事項一覧', submit_requests_path if user_signed_in? %></li>
      <li><%= link_to 'お願いされている事項一覧', inbox_submit_requests_path if user_signed_in? %></li>


  <% if user_signed_in? %>
    <li><%= link_to 'マイページ', edit_user_registration_path %></li>
    <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
  <% else %>
    <li><%= link_to 'ログイン', new_user_session_path %></li>
    <li><%= link_to 'サインアップ', new_user_registration_path %></li>
  <% end %>
  </ul>
</ul>
</li>
</ul>
</header>

<%= yield %>

</body>
</html>

<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% f.hidden_field :user_id %>
  <% f.hidden_field :charge_id %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
      <%= f.label :kind %><br>
  <select name="kind">
    <option value="探しています">
    探しています</option>
    <option value="提供できます">
    提供できます</option>
    <option value="その他">
    その他</option>
</select><br>
<div class="field">
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :charge %><br>
    <%= f.text_area :charge %>
  </div>

  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.datetime_select :deadline %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.select :status, Work.statuses.keys %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% f.hidden_field :user_id %>
  <% f.hidden_field :charge_id %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
  <%= f.label :kind %><br>
  <%= f.select :kind, [["探しています"], ["提供できます"], ["その他"]] %>
    <br>
<div class="field">
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.datetime_select :deadline%>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.select :status, Work.statuses.keys %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

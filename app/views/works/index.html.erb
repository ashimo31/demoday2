<p id="notice"><%= notice %></p>

<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Work</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="normalize.css">
</head>

	<div class="row">
        <br>
		<div class="col-md-9">
    	 <table class="table table-list-search">
         <div class="main-title">
    	     <h1>お願い一覧</h1>
         </div>
           <h4>特定のメンバーへの「探しています」「提供できます」「その他」お願いメッセージはこちらに掲載できます</h4>

        <div class="table-responsive">
          <table class="table table-hover">
              <thead>
                <tr align="center">
                    <th>依頼者</th>
                    <th>タイトル</th>
                    <th>いつまでに</th>
                    <th>内容</th>
                    <th>種類</th>
                    <th>担当者</th>
                    <th>ステータス</th>
                    <th colspan="3"></th>
                 </tr>
              </thead>

              <tbody>
                <% @works.each do |work| %>
                <tr>
                    <td><%= work.user.name %></td>
                    <td><%= work.title %></td>
                    <td><%= work.deadline.strftime("%Y年%m月%d日") %></td>
                    <td><%= work.content %></td>
                    <td><%= work.kind %></td>
                    <td><%= work.charge.name %></td>
                    <td><%= work.status%></td>
                  <% if work.charge.name == current_user.name%>
                    <td><%= link_to '詳細を見る', work %></td>
                    <td><%= link_to '編集する', edit_work_path(work) %></td>
                    <td><%= link_to '削除する', work, method: :delete, data: { confirm: '本当に削除しますか' } %></td>
                 <% end %>
                </tr>
              <% end %>
            </tbody>
            </table>


            <div class="actions">
            <p> <%= link_to "新しいお願いの登録へ", new_work_path, class: "btn btn-primary btn-block"  %></p>
            </div>

          </div>

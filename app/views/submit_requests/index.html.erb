<p id="notice"><%= notice %></p>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <title>依頼事項一覧</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="normalize.css">
</head>

<div class="row">
      <br>
  <div class="col-md-9">
     <table class="table table-list-search">
       <div class="main-title">
         <h1>お願いしている事項一覧</h1>
       </div>
         <h5>あなたから特定のメンバーへのお願いメッセージはこちらに掲載されています</h5>
      <div class="table-responsive">
        <table class="table table-hover">

    <thead>
      <tr>
        <th>依頼した相手</th>
        <th>依頼事項</th>
        <th>メモ</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @submit_requests.each do |submit_request| %>
        <tr>
          <td><%= submit_request.request_user.name %></td>
          <td><%= submit_request.work.title %></td>
          <td><%= submit_request.memo %></td>
          <td><%= link_to '詳細を見る', submit_request %></td>
          <td><%= link_to '修正する', edit_submit_request_path(submit_request) %></td>
          <td><%= link_to '削除する', submit_request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div class="actions">
  <p> <%= link_to "相手を選んで新しい依頼事項の提出へ", new_submit_request_path, class: "btn btn-primary btn-block"  %></p>
  </div>

</div>
